# ADLUKK Flutter App (in-repo)

This folder contains the Flutter project for ADLUKK.
Builds are produced automatically by GitHub Actions on every push to branch `feat/flutter-setup`.

Artifacts (APK / web build) are available in the Actions run summary -> Artifacts.
