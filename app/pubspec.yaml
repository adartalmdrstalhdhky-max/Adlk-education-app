name: adlukk_app
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Isar database (الإصدار الصحيح الذي يعمل)
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1

  # مزامنة HTTP
  http: ^1.1.0

  # إدارة الحالة
  provider: ^6.0.5

  # تخزين مؤقت
  shared_preferences: ^2.2.2

  # واجهات
  flutter_svg: ^2.0.7

  # أدوات مساعدة
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/
