name: adlik
description: A smart offline-first education platform for Yemen and the world.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.6
  
  # إدارة الحالة
  provider: ^6.0.5
  
  # قاعدة بيانات محلية OFFLINE
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # لعرض ملفات PDF
  flutter_pdfview: ^1.3.1
  path_provider: ^2.1.3
  
  # اتصال محلي / استيراد
  http: ^1.2.2
  
  # تخزين آمن للاحتياجات الأساسية
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  
  # 🎯 تم ربط مجلدات الأصول الآن:
  assets:
    - assets/images/
    - assets/fonts/
    - assets/data/
    
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  
