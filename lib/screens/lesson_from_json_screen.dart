import 'package:flutter/material.dart';
class LessonFromJsonScreen extends StatelessWidget {
@override
Widget build(BuildContext context) {
return Scaffold(
appBar: AppBar(title: Text('دروس النظام الرقمي')),
body: Center(child: Text('يتم تحميل البيانات من ملف JSON')),
);
}
}
