class SmartNotificationService {
  void scheduleReminder({
    required String lessonId,
    required String reason,
  }) {
    // placeholder: integrate with local notifications
    // مثال: "لديك أخطاء في حرف الألف، حاول مرة أخرى"
  }
}
